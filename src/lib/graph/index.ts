/**
 * LangGraph orchestration: router + Researcher / Analyst / Coder.
 * @see docs/Architecture.md — Agentic search flow
 */

// Stub: will wire StateGraph, router node, and agent nodes.
export async function routeAndRun(_query: string, _agentId?: string) {
  return { content: "", sources: [], suggestedActions: [] };
}
